"""Evaluation harness for resume agent workflow.

This module provides tools for evaluating and tuning prompts
with a local feedback loop using LangSmith SDK.

Usage:
    python -m evals.run_eval --stage drafting
    python -m evals.run_eval --stage drafting --dataset custom_examples.json
"""
